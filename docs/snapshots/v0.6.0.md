# v0.6.0 — M6 reconstructed snapshot

Status: `RECONSTRUCTED / ARTIFACT-PROVENANCE`

This record documents the user-supplied ZIP artifact. It does not claim to reproduce an original Git commit or original commit date.

## Artifact

- Source filename: `sh-sequence-engine-m6-0.6.0(1).zip`
- Milestone: M6
- Version: `0.6.0`
- SHA-256: `ece51d77dc902510e422967590ed0b322aac6bc9aee062f70d74e0c3651e721e`
- ZIP size: `294,419` bytes
- ZIP entries: `76`
- Non-directory files: `61`
- Uncompressed file bytes: `387,727`

## M6-specific artifact evidence

The artifact adds `M6-SPEC.md` and documents the M6 contract around:

- live overlay timeline;
- late real-theme header reveal;
- visual/background-only production frames;
- live HTML for meaningful UI;
- schema v4 overlays addressed through safe `data-shseq-key` values;
- whitelisted overlay transforms;
- `shseq:overlayprogress` events for later adapters;
- reduced-motion and fallback behavior.

The runtime manifest implementation observed in this artifact validates `shseq.runtime.manifest` schema version `4`.

## Runtime evidence

The artifact contains the manifest-driven runtime, state machine, frame validation, poster validation, performance policy, Canvas/sticky rendering, live HTML overlay structure, reduced-motion CSS, and fallback CSS. The included test checklist is retained as historical evidence rather than an independently executed QA report.

## Production caveat

`vendor/autoload.php` is explicitly documented by the artifact as a development PSR-4 loader because Composer CLI was unavailable in the artifact environment. This is a release-hardening item and must not be treated as optimized production Composer output.

## Provenance rule

No original Git SHA, original Git author timestamp, or original commit topology was supplied with this ZIP. Therefore this snapshot is intentionally labeled reconstructed and must not be represented as the historical upstream Git commit.
